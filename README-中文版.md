# Glass by Pickle (中文版)

Glass 是一款由 AI 驱动的桌面助手，旨在将您屏幕上的内容和您听到的对话，无缝转化为可操作的见解。

**当前版本采用智能LiteLLM路由架构，支持多个AI提供商，实现了功能与成本的最佳平衡。**

![演示](https://media.pickle.ai/glass-v2-banner.gif)

---

## ✨ 核心功能

*   **智能问答 (Ask)**: 随时通过快捷键`Ctrl+Shift+Y`提问。Glass能理解您屏幕上的内容，并结合上下文提供精准回答。无论是总结网页、解释代码，还是翻译文本，它都能胜任。
*   **实时会议助手 (Listen)**: 自动进行语音转录，并实时生成会议摘要、提取行动要点、总结关键决策。让您专注于沟通，不再分心于记笔记。
*   **隐私优先**: 应用在设计上完全不可见，不会出现在屏幕录制或截图中，充分保障您的隐私。
*   **智能路由**: 根据任务复杂度自动选择最合适的AI模型，简单任务使用经济模型，复杂任务使用高性能模型。

---

## 🚀 快速安装与配置

### 第 1 步：下载与安装

从 [Releases 页面](https://github.com/your-repo/releases) 下载适用于您操作系统的最新版本并安装。

### 第 2 步：AI服务配置

**Glass现在采用零配置启动方式！**

🎉 **无需任何配置**：
- 应用启动后直接进入主界面，无需输入API密钥
- 通过右上角的**LiteLLM状态灯**实时查看服务连接状态：
  - 🟢 **绿色**：LiteLLM服务连接正常，所有功能可用
  - 🟡 **黄色**：正在检查连接状态
  - 🔴 **红色**：LiteLLM服务暂时不可用

**Glass集成了Railway部署的LiteLLM服务，支持：**
- 📝 **文本任务**自动使用DeepSeek（高性价比）
- 🖼️ **图像分析**自动使用OpenAI（视觉能力）
- 🔄 **智能路由**根据任务复杂度选择最合适的模型

### 第 3 步：开始使用

应用启动后会直接显示主界面：
- 查看右上角状态灯确认LiteLLM服务状态
- 使用快捷键 `Ctrl+Enter` (Windows) 或 `Cmd+Enter` (Mac) 开始提问
- 点击系统托盘图标开始会议转录

---

## 📖 使用指南

### 1. 提问 (Ask)
*   **触发方式**: 使用快捷键 `Ctrl+Enter` (Windows) 或 `Cmd+Enter` (Mac) 来打开提问窗口。
*   **如何使用**:
    *   **常规提问**: 直接输入您的问题，如"帮我写一封邮件"。
    *   **基于屏幕内容提问**: 在提问时，Glass会自动捕获您当前的屏幕截图。您可以问："总结一下这个页面的内容"或"这段代码有什么问题？"

### 2. 实时转录 (Listen)
*   **触发方式**: 点击系统托盘/菜单栏中的Glass图标，选择 "Start Session"。
*   **如何使用**: 会议开始时启动会话即可。Glass会在后台自动记录和分析，会话结束后您可以在历史记录中查看摘要和转录文本。

---

## ⌨️ 快捷键大全

Glass提供了丰富的快捷键来提升使用效率。所有快捷键在Windows和Mac上都有对应版本：

### 🎯 核心功能快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **AI提问 (Ask)** | `Ctrl+Enter` | `Cmd+Enter` | 切换Ask窗口显示/隐藏，有内容时发送问题 |
| **会议转录 (Listen)** | `Ctrl+L` | `Cmd+L` | 切换Listen窗口显示/隐藏 |
| **手动截图** | `Ctrl+Shift+S` | `Cmd+Shift+S` | 立即截取屏幕截图用于分析 |

### 🖱️ 界面控制快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **切换可见性** | `Ctrl+\` | `Cmd+\` | 显示/隐藏所有Glass窗口 |
| **切换点击穿透** | `Ctrl+M` | `Cmd+M` | 开启/关闭鼠标事件穿透模式（让鼠标点击穿透Glass窗口） |

### 🏃 窗口移动快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **向上移动** | `Ctrl+Up` | `Cmd+Up` | 将Glass窗口向上移动一步 |
| **向下移动** | `Ctrl+Down` | `Cmd+Down` | 将Glass窗口向下移动一步 |
| **向左移动** | `Ctrl+Left` | `Cmd+Left` | 将Glass窗口向左移动一步 |
| **向右移动** | `Ctrl+Right` | `Cmd+Right` | 将Glass窗口向右移动一步 |

### 📐 窗口边缘快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **移至左边缘** | `Ctrl+Shift+Left` | `Cmd+Shift+Left` | 将窗口快速移动到屏幕左边缘 |
| **移至右边缘** | `Ctrl+Shift+Right` | `Cmd+Shift+Right` | 将窗口快速移动到屏幕右边缘 |
| **移至上边缘** | `Ctrl+Shift+Up` | `Cmd+Shift+Up` | 将窗口快速移动到屏幕上边缘 |
| **移至下边缘** | `Ctrl+Shift+Down` | `Cmd+Shift+Down` | 将窗口快速移动到屏幕下边缘 |

### 🖥️ 多显示器快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **切换到显示器1** | `Ctrl+Shift+1` | `Cmd+Shift+1` | 将窗口移动到第一个显示器 |
| **切换到显示器2** | `Ctrl+Shift+2` | `Cmd+Shift+2` | 将窗口移动到第二个显示器 |
| **切换到显示器3** | `Ctrl+Shift+3` | `Cmd+Shift+3` | 将窗口移动到第三个显示器 |
| **...** | `Ctrl+Shift+N` | `Cmd+Shift+N` | 支持最多9个显示器 |

### 📜 响应导航快捷键

| 功能 | Windows | Mac | 说明 |
|------|---------|-----|------|
| **上一个响应** | `Ctrl+[` | `Cmd+[` | 查看上一个AI响应 |
| **下一个响应** | `Ctrl+]` | `Cmd+]` | 查看下一个AI响应 |

### 💡 快捷键使用技巧

1. **Ask功能最佳实践**：
   - 使用 `Ctrl+Enter` (Windows) 或 `Cmd+Enter` (Mac) 快速打开Ask窗口
   - 在Ask窗口中，同样的快捷键可以快速发送您的问题

2. **窗口管理技巧**：
   - 使用 `Ctrl+\` 快速隐藏/显示所有Glass窗口
   - 在多显示器环境中，使用 `Ctrl+Shift+数字` 快速切换显示器

3. **高效移动**：
   - 使用方向键进行精确移动
   - 使用 `Shift+方向键` 快速移动到屏幕边缘

4. **点击穿透模式**：
   - 使用 `Ctrl+M` 开启点击穿透，让鼠标点击穿透Glass窗口到下面的应用
   - 再次按下相同快捷键关闭点击穿透模式，恢复对Glass窗口的正常操作
   - 🎯 **智能置顶**：Glass窗口只在有功能窗口显示时才置顶，平时不会阻挡其他应用

5. **快捷键优先级**：
   - 注意：`Ctrl+Shift+Up/Down` 主要用于窗口边缘移动
   - 在有多个功能使用相同快捷键时，窗口移动功能优先生效

---

## 🛠️ 开发人员：从源码运行

如果您想从源代码运行或进行二次开发：

### 第 1 步：克隆仓库
```bash
git clone https://github.com/jason-michael/glass.git
cd glass
```

### 第 2 步：安装依赖
```bash
npm install
```

### 第 3 步：构建前端应用
```bash
cd pickleglass_web
npm install
npm run build
cd ..
```

### 第 4 步：运行应用
```bash
npm start
```

---

## 🔧 故障排除

### 常见启动问题

#### 问题1：字符编码乱码
**现象**：PowerShell中看到乱码如"馃殏 Railway閰嶇疆"
**解决方案**：
```powershell
chcp 65001
npm start
```

#### 问题2：数据库Schema错误
**现象**：错误信息"table users has no column named display_name"
**解决方案**：删除旧数据库文件重新创建
```powershell
Remove-Item -Path "$env:APPDATA\Glass\pickleglass.db*" -Force -ErrorAction SilentlyContinue
npm start
```

#### 问题3：前端构建缺失
**现象**：错误信息"Frontend build directory not found"
**解决方案**：
```bash
cd pickleglass_web
npm install
npm run build
cd ..
npm start
```

#### 问题4：端口被占用
**现象**：应用启动失败，提示端口冲突
**解决方案**：应用会自动寻找可用端口，如仍有问题请重启系统

---

## ❓ 常见问题 (FAQ)

*   **问：我需要一台性能强大的电脑吗？**
    *   答：完全不需要。所有AI计算都通过云端API完成，对您的本地电脑配置没有要求。

*   **问：我的数据安全吗？**
    *   答：您的数据安全通过端到端加密保护。当您使用服务时，相关数据（如截图和问题文本）会被发送到对应的AI提供商进行处理。我们建议您查阅并遵守其隐私政策。

*   **问：应用启动后看到红色状态灯怎么办？**
    *   答：红色状态灯表示LiteLLM服务暂时不可用。这可能是：
        - 网络连接问题，请检查您的网络
        - Railway服务临时维护，通常几分钟后会恢复
        - 如果长时间显示红色，可以尝试重启应用

*   **问：使用这个服务会很贵吗？**
    *   答：Glass采用智能LiteLLM路由架构，会根据任务类型自动选择最经济的模型：
        - 简单文本任务自动路由到DeepSeek（成本极低）
        - 图像分析任务使用OpenAI（按需计费）
        - 总体成本比直接使用顶级模型节省80%以上

*   **问：我需要配置API密钥吗？**
    *   答：完全不需要！Glass现在采用零配置启动方式：
        - 启动应用即可直接使用所有功能
        - 通过Railway LiteLLM服务自动处理所有AI请求
        - 右上角状态灯会显示服务连接状态

*   **问：应用启动后我在哪里找到功能入口？**
    *   答：
        - **Ask功能**：使用快捷键`Ctrl+Enter`随时提问
        - **Listen功能**：点击系统托盘图标选择"Start Session"
        - **状态监控**：查看右上角的LiteLLM状态灯

---

如果您有任何问题或建议，欢迎随时提出！